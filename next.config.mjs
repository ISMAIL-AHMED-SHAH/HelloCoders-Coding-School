/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.claudinary.com', 'media.licdn.com', 'avatars.githubusercontent.com']
    }
};

export default nextConfig;
